# Copy this file to .env and fill in your values:
#   cp env.txt .env
#
# Required for Tenable Cloud Security GraphQL API
TENABLE_API_URL=https://app.tenable.com/api/graph
TENABLE_CUSTOMER_ID=your-tenable-customer-id
TENABLE_API_TOKEN=your-tenable-api-token

# Required when using --send-to-splunk (optional for file-only runs)
SPLUNK_HEC_URL=https://your-splunk-host:8088/services/collector/event
SPLUNK_HEC_TOKEN=your-splunk-hec-token
SPLUNK_HEC_VERIFY_SSL=false

# Splunk event metadata (optional; defaults shown)
SPLUNK_INDEX=tenable
SPLUNK_SOURCE=demo-tenable-correlation
SPLUNK_SOURCETYPE=httpevent
